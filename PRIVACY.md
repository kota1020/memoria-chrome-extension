# memoria for Chrome — Privacy Policy

最終更新: 2026-09-04
公開者: Kotaro Murakami
連絡先: kotaro.murakami@infozerogrid.com
対象: Chrome Web Store で配布する memoria for Chrome（バージョン 2.0.0 以降）

この拡張機能は、利用者自身のMacの中で動く memoria（個人用の記憶レイヤー）に対して、
「いまブラウザで何を見ていたか」を**正確な形で**渡すためのものです。

memoria は画面を撮って文字認識（OCR）で読んでいるため、ブラウザの中身はほとんど当たりません。
実測では、記録された本文が `Nunrau Cumael Pwvsy. · -.м..•` のような読めない文字列になり、
タイトルも `Memoria Context Faucet -…` のように途中で切れていました。
ブラウザ自身が正確な値を渡すことで、この推測が実測に置き換わります。それがこの拡張機能の唯一の目的です。

## 取得するもの

利用者が開いているページについて、次を取得します。

| 項目 | 内容 |
|---|---|
| URL | ページの完全なURL |
| タイトル | ページのタイトル（省略なし） |
| 本文テキスト | ページの本文。最大20,000文字。入力欄に入力した値は含まれません |
| 言語・参照元 | `<html lang>` と referrer |
| 滞在時間 | そのページが**画面に見えていた**開始時刻・終了時刻・秒数 |

対象は `http://` と `https://` のすべてのページです。

## 取得しないもの

- **パスワード欄があるページの本文**。ログイン画面・銀行・決済など、パスワード入力欄が
  1つでもあるページでは本文を読み取りません（URLとタイトルのみ）。設定に関係なく常にこの動作です。
- **入力欄・フォームに入力した値**。取得するのは表示されている文章だけです。
- パスワード、クレジットカード情報、Cookie、マイク音声、カメラ映像、画面録画。
- シークレットウィンドウの情報。Chromeの既定でこの拡張機能はシークレットでは動きません。
- 利用者が設定で除外したサイト（後述）。

## 利用者が止められること

- **全体のオン／オフ** — ツールバーのアイコンから切り替えます。オフの間は一切読み取りません。
- **本文を渡さないサイト** — ホスト名を登録すると、そのサイトはURLとタイトルだけになります。
- **何も渡さないサイト** — ホスト名を登録すると、そのサイトは一切記録しません。

## 送信先

送信先は、利用者自身のMacの中で動く memoria の受け口
（既定 `http://127.0.0.1:4319/ingest/page`）だけです。これはループバックアドレスであり、
通信は端末の外へ出ません。

**設定画面でも `127.0.0.1` 以外は指定できません**（コードで拒否しています）。
外部サーバー、当方のサーバー、クラウド、広告ネットワーク、解析サービスへは一切送信しません。
拡張機能には第三者の解析コードもトラッキングコードも含まれていません。
リモートからコードを読み込むこともありません。

## 保存

拡張機能が保存するのは、利用者の設定（オン／オフ、除外リスト、送信先）だけです。
ページの内容は、memoria へ渡すまでの一時的な待ち行列（最大200件、メモリ上）にのみ置かれ、
渡し終えた時点で消えます。memoria が動いていない間は待ち行列に残り、
200件を超えると古いものから捨てられます。

## 販売・共有

取得したいかなる情報も、販売しません。第三者と共有しません。
拡張機能の中心的機能と無関係な目的には使用しません。信用調査を含む与信目的には使用しません。

## 削除

拡張機能を削除すれば、それ以降の取得は完全に止まり、設定と待ち行列も消えます。
すでに memoria へ渡した記録は利用者のMac上の memoria が持っているため、
その削除は memoria 本体で行ってください。

## 権限を使う理由

- `storage` — 利用者の設定（オン／オフ、除外リスト、送信先）を覚えるため。
- `alarms` — memoria が起動していない間に溜まった分を、定期的に渡し直すため。
- ホスト権限 `http://127.0.0.1:4319/*` — 利用者自身の端末で動く memoria へ渡すため。
  これ以外のホスト権限は要求しません。
- コンテンツスクリプト（`http://*/*` `https://*/*`）— どのページを見ていたかを記録するのが
  この拡張機能の目的であるため、対象を特定サイトに限定できません。
  ページの表示や動作は一切変更しません。
