# Memoria Context Faucet — Privacy Policy

最終更新: 2026-09-03
公開者: Kotaro Murakami
連絡先: kotaro.murakami@infozerogrid.com
対象: Chrome Web Store で配布する Memoria Context Faucet（バージョン 1.0.0 以降）

Memoria Context Faucet は、利用者自身のMacの中で動く Memoria（個人用の記憶レイヤー）へ
問い合わせて、いま開いているページに関係する自分の記憶をサイドパネルに表示する拡張機能です。
Memoria が動いていない環境では、何も表示せず、何も送信しません。

## 読むもの

サイドパネルを開いている間だけ、次の3つを読みます。

1. **現在のタブのタイトル**
2. **現在のタブのホスト名**（URL のうちホスト部分のみ。パスやクエリは使いません）
3. **利用者が明示的に選択したテキスト（最大800文字）** — 選択していなければ何も読みません

## 読まないもの

ページ本文、パスワード、フォームの入力値、クレジットカード情報、Cookie、閲覧履歴、
位置情報、マイク音声、カメラ映像、画面録画、キーストロークは取得しません。
サイドパネルを閉じている間は、いかなるページの情報も読みません。

## 送信先

上記3つの送信先は、利用者自身のMacの中で動く Memoria 読み取りAPI
（`http://127.0.0.1:4319`）だけです。これはループバックアドレスであり、通信は端末の外へ出ません。

外部サーバー、当方のサーバー、クラウド、広告ネットワーク、解析サービスへは一切送信しません。
拡張機能には第三者の解析コードもトラッキングコードも含まれていません。
リモートからコードを読み込むこともありません。

## 保存

拡張機能はページのタイトル、ホスト名、選択テキスト、表示した記憶のいずれも保存しません。
`chrome.storage` も使用していません。

## 販売・共有

取得したいかなる情報も、販売しません。第三者と共有しません。
拡張機能の中心的機能と無関係な目的には使用しません。信用調査を含む与信目的には使用しません。

## 削除

拡張機能を削除すれば、それ以降の読み取りは完全に止まります。
記憶そのものは利用者のMac上の Memoria が保持しているため、その削除は Memoria 本体で行ってください。

## 権限を使う理由

- `activeTab` — 利用者がサイドパネルを開いた現在のタブから、選択テキストを読むため。
- `scripting` — 選択テキストを読み取る短い関数を、現在のタブでのみ実行するため。
- `tabs` — サイドパネルが「いま見ているページ」を追えるようにするため（タイトルとホスト名のみ使用）。
- `sidePanel` — 記憶をサイドパネルに表示するため。
- ホスト権限 `http://127.0.0.1:4319/*` — 利用者自身のMacの Memoria 読み取りAPIへ接続するため。
  これ以外のホスト権限は要求しません。
